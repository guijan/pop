/* This code is confusing because it's not consistent with the rest of the
 * language. It also confuses through its repeated inversion of the meanings
 * of true and false.
 */

#define TRUE 0
#define FALSE 1

if ((ch = getchar()) == EOF)
	not-eof = FALSE;



